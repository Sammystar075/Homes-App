import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Listing4Page } from './listing4.page';

describe('Listing4Page', () => {
  let component: Listing4Page;
  let fixture: ComponentFixture<Listing4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Listing4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});


